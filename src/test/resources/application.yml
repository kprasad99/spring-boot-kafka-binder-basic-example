spring:
  cloud:
    stream:
      kafka:
        binder:
          brokers:
          - localhost
      bindings:
        kp-in-test-channel:
          destination: kp-hello-test-topic
          content-type: text/plain
        kp-out-test-channel:
          destination: kp-hello-test-topic
          content-type: text/plain